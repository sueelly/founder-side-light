# 터미널 면접 하네스 작업 규칙

먼저 README.md와 docs/terminal-interview-spec.md, docs/terminal-interview-plan.md를 읽습니다.

- 이 워크트리만 수정합니다. 참고 원본 `~/Downloads/hr-training-runtime 2`는 읽기 전용입니다.
- 사용자 합의에 따라 확정 후보군의 과거·현재·말투 자료만 지원자 역할용으로 가져올 수 있습니다. 미래 시나리오·근속 수치·운영 상태·자격 증명은 가져오지 않습니다.
- 총 4명, 첫 면접관은 사람이고 이후 3명은 Claude입니다. 지원자는 모두 Claude이며 기술면접을 통과한 인성면접입니다.
- 첫 init은 확정 candidate_pools.json으로 P07 먼저, 나머지 세 구간 각 1명과 무작위 순서를 고정합니다. 재추첨·모델 재분류를 하지 않습니다. 구간은 어느 모델에도 전달하지 않습니다.
- 카카오와 운영 서버를 사용하지 않습니다. 연결 설정·화면 조작·로그인/제출/접수 API가 없습니다.
- 하네스 하나가 발언 차례·시간·기록을 관리합니다. 모델은 허용 입력으로 발화 하나만 만들며 파일·화면·검색 도구를 받지 않습니다.
- 사람 최대 1800초, AI 최대 1200초. 시작 기록 시각과 마감은 고정하며 재시작으로 연장하지 않습니다. 마감 후 재개하면 종료부터 처리합니다.
- 시작은 `OOO님 면접 시작하겠습니다`, 마지막 발화는 `수고하셨습니다`입니다.
- 시작은 README의 설치 명령과 `python -m harness start`를 사용합니다. 별도 setup 문서·OS별 실행 파일·자동 설치·시작 시 테스트 실행을 추가하지 않습니다. 모델 실행 전 본인 Claude 인증 검사는 유지하며, 준비된 환경에서는 인사이트 작성·확인부터 진행합니다.
- 기준 원본은 세션 insights.md입니다. 사용자가 명시한 내용만 수정하며 자동 인사이트 추출·자동 학습을 추가하지 않습니다.
- 매 후보의 평가와 인사이트 변경 여부는 실제 사용자 입력을 기다립니다. 실제 사용자의 판단을 만들어 대신 입력하지 않습니다.
- 상태·기록·확정 평가·사본·순위는 하네스 명령으로 다룹니다. JSON을 직접 편집해 진행하지 않습니다. 기록 변경·손상은 확인 없이 덮어쓰지 않습니다.
- 지원자에게 인사이트·평가·순위·다른 후보를, 면접관과 순위 모델에 후보 전용 설정을 전달하지 않습니다.
- 네 번째 피드백 뒤 최종 기준으로 로컬 순위를 저장합니다. 외부 제출은 없습니다.
- 합성 테스트의 사람 입력은 별도 테스트 세션에만 사용합니다. 실제 면접·평가가 완료됐다고 보고하지 않습니다.
- 코드 변경 후 `python -m pytest -q`, `python -m harness doctor`를 실행합니다. 합성 검사·실제 Claude·Mac/Windows 실연결 확인을 구분합니다.

## 채팅과 시연 출력

- 사용자가 대화를 보기 쉽게 실행 명령과 도구 출력을 짧게 유지합니다. 긴 코드를 heredoc이나 인라인 명령에 통째로 넣지 말고 이 워크트리의 스크립트 파일에 작성한 뒤 짧은 명령으로 실행합니다.
- 시연을 전달하는 채팅에는 화자와 실제 생성된 발화를 우선 표시합니다. 반복 타이머, 호출별 소요 시간, 내부 메타데이터와 전체 경로를 매 발화마다 붙이지 않습니다.
- 상세 진단과 검증 로그는 별도 로컬 파일에 보관하고 결과와 필요한 오류만 간단히 보고합니다. 오류·중단·사용자 입력 요청은 숨기지 않습니다.
- 파일 조회는 필요한 부분만 출력하고, 이미 읽은 문서를 변경 확인 없이 반복해서 전체 출력하지 않습니다.
- 이 규칙은 실행·응답 방식을 정하는 지침입니다. Codex 앱의 명령 카드 자체를 숨기는 환경 설정이라고 설명하지 않습니다.
