# 터미널 면접 시작

Mac에서는 `실행.command`, Windows에서는 `실행.bat`를 여세요.
시작 프로그램이 OS를 감지하고 Python 환경과 필요한 패키지를 설치합니다.
Claude 설치·본인 로그인은 자동 확인하고, 로그인이 필요할 때만 브라우저 인증을 안내합니다.
API 키나 운영 서버 로그인 정보는 필요하지 않습니다.

## 진행

1. 열리는 `insights.md`에 면접 기준을 작성합니다.
2. 터미널에 표시된 기준을 확인하고 `시작`을 입력합니다. 파일을 수정했다면 Enter로 다시 표시합니다.
3. 첫 면접은 직접 질문을 입력합니다. 다음 세 면접은 Claude 면접관과 지원자의 대화를 지켜봅니다.
4. 매 면접 종료 후 `feedback.md`의 종합 평가·근거·우려·확인할 점을 작성합니다.
5. 인사이트를 바꿨으면 `수정`, 그대로면 `그대로`를 입력합니다. 다음 면접도 기준 확인 후 시작합니다.
6. 네 번째 평가 후 `ranking.md`와 `ranking.json`에 최종 순위를 저장하고 끝납니다.

질문은 평문 또는 `q 질문`으로 입력합니다. `/last`로 마무리를 요청하고 후보 답을 받은 뒤 `/end`로
종료할 수 있습니다. 두 명령은 사람 면접에서만 사용합니다. `/status`와 `/retry`는 모든 면접에서 사용할 수 있습니다.
모델이 답하는 동안 접수하지 않은 질문은 입력란에 남고 자동 발송되지 않습니다.

사람 면접은 최대 30분, AI 면접은 최대 20분입니다. Ctrl+C로 중단한 시간도 마감에 포함합니다.
같은 파일을 다시 실행하면 이어지며, 마감이 지났으면 종료부터 기록합니다.
기본 위치는 `.runtime/terminal-light`입니다. 기존 카카오 세션은 보존하고 자동 전환하지 않습니다.

## 이 문서를 읽는 에이전트의 실행 절차

- `AGENTS.md`와 `README.md`를 따릅니다. 사용자에게 명령을 나열하고 끝내지 말고 실제 입력을 받을 터미널을 엽니다.
- Mac: `open -a Terminal ./실행.command`
- Windows: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-windows.ps1 -OpenWindow`
- 카카오·화면 조작·운영 서버 준비는 하지 않습니다. 열린 터미널에서 실제 사용자가 질문·시작 확인·평가를 입력합니다.
- 사용자 판단을 대신 입력하거나 JSON을 편집해 단계를 넘기지 않습니다. 실행 중인 하네스를 중복 시작하지 않습니다.
- 사용자가 인사이트 수정을 지시하면 지정한 내용만 현재 세션의 `insights.md`에 반영합니다.
- 설치 오류는 `.bootstrap/install.log`, 로컬 검사 결과는 `.bootstrap/background-checks.log`에서 확인합니다.
- 로컬 검사, 실제 Claude 응답, 실제 사용자 실습을 구분해 보고합니다.

원문·확정 사본을 잘못 편집했다면 자동 덮어쓰기하지 않습니다. 사용자가 확정 상태로 복원하기로 한 경우에만
`python -m harness --session <세션 경로> restore-records --confirmed-restore`를 실행합니다.
